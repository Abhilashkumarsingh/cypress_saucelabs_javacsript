{
  "name": "cypress_javascript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type":"commonjs",
  "scripts": {
    "browser:chrome": "npx cypress run --browser=chrome --headless --reporter mocha-allure-reporter",
    "browser:electron": "npx cypress run --browser=electron --headless --reporter mocha-allure-reporter",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open  allure-report"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "allure-commandline": "^2.30.0",
    "allure-cypress": "^3.0.0-beta.8",
    "cypress": "^13.13.2",
    "mocha-allure-reporter": "^1.4.0"
  }
}
